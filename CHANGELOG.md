# Changelog

## 1.1.4 - 2026-09-13
- 更新窗口升级：打开就能看到这个版本更新了什么；不想升可以「跳过此版本」或「稍后提醒我」，也能勾选「自动下载并安装以后的更新」。
- 更新窗口不再打断你正在打的字（不会再因为按回车误触更新）。
- 修复主窗口偶尔跑到屏幕外或缩成一小块的问题。

## 1.1.3 - 2026-09-13
- 「关于」页焕新：应用图标、版本、简介、许可证居中一页，检查更新和仓库、作者链接一眼可找。
- 更新体验更省心：发现新版本弹窗询问，点「立即更新」后原地显示下载进度，失败会说明原因并可一键重试。
- 「检查更新」更可靠：关闭弹窗提醒后仍可手动检查；点了「暂不更新」的版本不再重复弹窗打扰。
- 修复启动和点菜单栏图标时可能长时间无响应的问题。
- 总览页「启动新实例」改名「启动新微信」，更好懂。
- 补上 MIT 开源许可证。

## 1.1.2 - 2026-09-12
- 设置里的开关改小一号，和系统设置里的样式一致，看起来更自然。

## 1.1.1 - 2026-09-12
- 新开的微信副本更省空间，而且几乎秒开（不再整份复制）。
- 维护页新增「重建现有副本」：旧副本一键转换成省空间的方式，回收空间；只处理未运行的副本，账号数据不受影响。

## 1.1.0 - 2026-09-12
- 设置窗口改成 macOS 原生的左右两栏：左边是「总览 / 账号 / 维护 / 关于」四个栏目，整行都能点，右边是对应内容。
- 「账号」页按微信窗口逐一展示卡片：登录的账号、有没有记住、数据占了多少，一眼可读；每张卡片只显示当前需要做的那个操作，其余动作收进右侧菜单。
- 窗口名称点击即可修改，回车保存。
- 登录账号和记住的账号对不上时，发系统通知提醒串号（可在维护页开关）。
- 微信升级后，副本会提前标注「待同步」，打开前心里有数。
- 耗时操作都在后台进行，过程中界面保持流畅，按钮不会误触重复。

## 1.0.1 - 2026-02-27
- 应用对外名称统一为 `WeClone`（不改内部可执行名，避免兼容性风险）。
- DMG 输出文件名改为固定 `WeClone.dmg`（不再附带日期时间）。
- 旧产物清理策略明确：分发只使用 `WeClone.app` 与 `WeClone.dmg`。
