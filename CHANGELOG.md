# Changelog

## 1.1.1 - 2026-09-12
- 克隆微信改用 APFS 写时复制（clonefile）：副本创建从几十秒缩短到 1 秒内，新增占用从整包约 640MB 降到约三分之一以下；非 APFS 卷自动回退普通拷贝。
- 维护页新增「重建现有副本」：把旧版创建的完整拷贝副本重建为写时复制克隆，回收空间（只处理未运行的副本）。

## 1.1.0 - 2026-09-12
- 设置界面重构为 macOS 原生双栏布局（总览/账号/维护/关于），侧栏整行可点。
- 账号页改为状态驱动的窗口卡片：状态点、大白话状态说明、当前该做的动作作主按钮，次要操作收进菜单；「数据目录」独立窗口内联。
- 新增串号提醒：窗口登录账号与记住账号不一致时发系统通知（可在维护页开关）。
- 新增数据目录体积显示：账号卡片展示聊天数据占用，后台统计不卡界面。
- 新增副本「待同步」标识：微信升级后提前告知副本下次启动会自动重建。
- 新增自动更新：检查 GitHub Releases（API 限流自动回退网页重定向），弹窗询问后下载 DMG 自动换装并重启；关于页可手动检查、可开关。
- DMG 产物改为带版本号命名（WeClone-x.y.z.dmg），耗时操作全部移入后台线程。

## 1.0.1 - 2026-02-27
- 应用对外名称统一为 `WeClone`（不改内部可执行名，避免兼容性风险）。
- DMG 输出文件名改为固定 `WeClone.dmg`（不再附带日期时间）。
- 旧产物清理策略明确：分发只使用 `WeClone.app` 与 `WeClone.dmg`。
